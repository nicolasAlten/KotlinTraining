package Strings

class StringIteration {
    fun iterate(){
        var user = "user"
        var password = "password"

        print(user[1])
        user.toList()

        user += ""

        var newConcatenatedVal = "$user $password"

    }
}